/**
 * Practices are intentionally independent from reigns. Add a practice only
 * when its owner, provenance, and RaidGuild relationship language are known.
 */
export const PRACTICES: readonly unknown[] = [];
